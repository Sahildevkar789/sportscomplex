<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>All Turfs</title>
  <style>
    /* Global Styles */
    body {
      background-color: #121212; /* Dark mode */
      color: #ffffff;
      font-family: Arial, sans-serif;
      text-align: center;
      margin: 0;
      padding: 20px;
    }

    h1 {
      animation: fadeInDown 1s ease-in-out;
      margin-bottom: 20px;
    }

    /* Table Styling */
    table {
      width: 80%;
      margin: auto;
      border-collapse: collapse;
      background: #1f1f1f;
      border-radius: 10px;
      box-shadow: 0px 4px 10px rgba(255, 255, 255, 0.1);
      overflow: hidden;
      animation: fadeInUp 1s ease-in-out;
    }

    th, td {
      padding: 12px;
      border-bottom: 1px solid #444;
      text-align: left;
    }

    th {
      background: #333;
    }

    tr:hover {
      background: #222;
      transition: 0.3s;
    }

    /* Action Buttons */
    a {
      text-decoration: none;
      font-weight: bold;
      padding: 8px 12px;
      border-radius: 5px;
      transition: 0.3s;
      display: inline-block;
    }

    a[href*="edit"] {
      background: #4caf50;
      color: #fff;
    }

    a[href*="edit"]:hover {
      background: #45a049;
    }

    a[href*="delete"] {
      background: #e74c3c;
      color: #fff;
    }

    a[href*="delete"]:hover {
      background: #c0392b;
    }

    /* Create New Turf Button */
    .create-btn {
      display: inline-block;
      margin-top: 20px;
      background: #ffffff;
      color: #000;
      padding: 10px 15px;
      font-size: 1rem;
      font-weight: bold;
      border-radius: 5px;
      transition: 0.3s;
    }

    .create-btn:hover {
      background: #ddd;
    }

    /* Animations */
    @keyframes fadeInDown {
      from { opacity: 0; transform: translateY(-20px); }
      to { opacity: 1; transform: translateY(0); }
    }

    @keyframes fadeInUp {
      from { opacity: 0; transform: translateY(20px); }
      to { opacity: 1; transform: translateY(0); }
    }
    nav {
      background: #1f1f1f;
      padding: 15px;
      border-radius: 10px;
      box-shadow: 0px 4px 10px rgba(255, 255, 255, 0.1);
      animation: fadeInUp 1s ease-in-out;
    }
hr{
  display: block;
  background: #5c8bda;
  height: 0.25rem;
  width: 100%;
  border: none;
  margin: 1.125rem 0 1.875rem 0;
}
    ul {
      list-style: none;
      padding: 0.7rem;
      margin: 0px;
      display: flex;
      justify-content: center;
      gap: 20px;
    }

    li {
      display: inline;
    }

    a {
      text-decoration: none;
      color: #ffffff;
      font-weight: bold;
      font-size: 1.1rem;
      padding: 10px 15px;
      transition: 0.3s;
      border-radius: 5px;
    }

    a:hover {
      background: #ffffff;
      color: #000;
    }
    .logo{
      margin-right: 20px;
    }
    .title {
      display: flex;
      align-items: center;
      justify-content: center;
      gap: 10px; /* Space between logo and text */
      text-align: center;
      font-size: 1.8rem; /* Adjust as needed */
  }
  
  .logo {
      height: 40px; /* Adjust logo size */
  }
  
  </style>
</head>
<body>
  <nav>   <h1 class="title">
    <img src="../style/assests/fcritlogo.png" alt="Fr. Agnels Sports Complex" width="40" class="logo">
    Fr. Agnels Sports Complex
</h1>

   <hr> <ul>
      <li><a href="/book">Home</a></li>
      <li><a href="/admin/turfs">Manage Turfs</a></li>
      <li><a href="/admin/bookings">View All Bookings</a></li>
      <li><a href="/admin/data">Memberships</a></li>
      <li><a href="/logout">Logout</a></li>
      
    </ul>
  </nav> 

  <h1>All Turfs</h1>

  <table>
    <thead>
      <tr>
        <th>Title</th>
        <th>Price Per Hour</th>
        <th>Actions</th>
      </tr>
    </thead>
    <tbody>
      <% turfs.forEach(turf => { %>
        <tr>
          <td><%= turf.title %></td>
          <td>₹<%= turf.pricePerHour %></td>
          <td>
            <a href="/admin/turfs/<%= turf._id %>/edit">Edit</a>
            <a href="/admin/turfs/<%= turf._id %>/delete">Delete</a>
          </td>
        </tr>
      <% }); %>
    </tbody>
  </table>

  <a href="/admin/turfs/new" class="create-btn">Create New Turf</a>

</body>
</html>
